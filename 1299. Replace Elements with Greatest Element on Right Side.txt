class Solution {
    public int[] replaceElements(int[] arr) {
        int max1 = -1;

        for(int i = arr.length-1; i>=0 ;i--){
            int current = arr[i];
            arr[i] = max1;
            max1 = Math.max(current,max1);
        }
        return arr;
    }
}